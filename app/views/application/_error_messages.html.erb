<% if resource.errors.any? %>
  <div class="text-red-600 uppercase py-1 mb-1 font-bold text-xs">
    <ul>
      <% resource.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>